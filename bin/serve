#!/usr/bin/env sh
set -e
export RACK_ENV=${RACK_ENV:-development}
docker-compose up -d --build --force-recreate funk-cleaner
docker-compose up -d --build --force-recreate funk-api
docker attach kontena-funk-api
